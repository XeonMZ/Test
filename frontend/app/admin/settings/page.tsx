import { AdminSettingsPage } from '@/features/admin-owner/admin-console';

export const metadata = { title: 'Admin — Settings — SJT' };

export default function Page() {
  return <AdminSettingsPage />;
}
