import { DashboardPage } from '@/features/admin-owner/components';
export const metadata = { title: 'Owner Dashboard — SJT' };
export default function Page() { return <DashboardPage scope="owner" />; }
